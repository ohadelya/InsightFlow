// Report layer scaffolding for future report generation and export functionality.
// This module is intentionally minimal to preserve current application behavior.

export function getReportPlaceholder() {
  return {
    name: "InsightFlow Report Layer",
    status: "placeholder",
  };
}
